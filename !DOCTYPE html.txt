<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CircuitCraft | Custom PCB Design Services</title>
    <style>
        :root {
            --primary: #00ff88;
            --dark: #121212;
            --gray: #1e1e1e;
            --text: #ffffff;
            --text-muted: #a0a0a0;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background-color: var(--dark);
            color: var(--text);
            line-height: 1.6;
        }

        header {
            background-color: var(--gray);
            padding: 1.5rem 5%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 2px solid var(--primary);
        }

        .logo {
            font-size: 1.8rem;
            font-weight: bold;
            color: var(--primary);
            text-decoration: none;
        }

        nav ul {
            list-style: none;
            display: flex;
            gap: 2rem;
        }

        nav a {
            color: var(--text);
            text-decoration: none;
            font-weight: 500;
            transition: color 0.3s;
        }

        nav a:hover {
            color: var(--primary);
        }

        .btn {
            background-color: var(--primary);
            color: var(--dark);
            padding: 0.8rem 1.5rem;
            border: none;
            border-radius: 4px;
            font-weight: bold;
            cursor: pointer;
            text-decoration: none;
            transition: transform 0.2s, background 0.3s;
        }

        .btn:hover {
            transform: translateY(-2px);
            background-color: #00cc6a;
        }

        .hero {
            padding: 8rem 5%;
            text-align: center;
            background: linear-gradient(rgba(18, 18, 18, 0.9), rgba(18, 18, 18, 0.9)), url('https://images.unsplash.com/photo-1518770660439-4636190af475?ixlib=rb-1.2.1&auto=format&fit=crop&w=1920&q=80') center/cover;
        }

        .hero h1 {
            font-size: 3.5rem;
            margin-bottom: 1rem;
        }

        .hero h1 span {
            color: var(--primary);
        }

        .hero p {
            font-size: 1.2rem;
            color: var(--text-muted);
            max-width: 600px;
            margin: 0 auto 2.5rem;
        }

        .services {
            padding: 5rem 5%;
            background-color: var(--gray);
        }

        .section-title {
            text-align: center;
            font-size: 2.5rem;
            margin-bottom: 3rem;
            color: var(--text);
        }

        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
        }

        .card {
            background-color: var(--dark);
            padding: 2rem;
            border-radius: 8px;
            border-top: 4px solid var(--primary);
            transition: transform 0.3s;
        }

        .card:hover {
            transform: translateY(-5px);
        }

        .card h3 {
            margin-bottom: 1rem;
            font-size: 1.5rem;
        }

        .card p {
            color: var(--text-muted);
        }

        .cta {
            padding: 5rem 5%;
            text-align: center;
        }

        footer {
            background-color: var(--gray);
            text-align: center;
            padding: 2rem;
            color: var(--text-muted);
            border-top: 1px solid #333;
        }

        @media (max-width: 768px) {
            header {
                flex-direction: column;
                gap: 1rem;
            }
            .hero h1 {
                font-size: 2.5rem;
            }
        }
    </style>
</head>
<body>

    <header>
        <a href="#" class="logo">&lt;/CircuitCraft&gt;</a>
        <nav>
            <ul>
                <li><a href="#services">Services</a></li>
                <li><a href="#portfolio">Portfolio</a></li>
                <li><a href="#contact">Contact</a></li>
            </ul>
        </nav>
        <a href="#quote" class="btn">Get a Quote</a>
    </header>

    <section class="hero">
        <h1>Custom <span>PCB Design</span> for Innovators</h1>
        <p>From schematic capture to mass production, we turn your hardware ideas into reality with high-quality, manufacturable printed circuit boards.</p>
        <a href="#quote" class="btn">Start Your Project</a>
    </section>

    <section id="services" class="services">
        <h2 class="section-title">Our Capabilities</h2>
        <div class="grid">
            <div class="card">
                <h3>Schematic Capture</h3>
                <p>Translating your concepts and system block diagrams into precise, error-free schematics ready for layout.</p>
            </div>
            <div class="card">
                <h3>PCB Layout</h3>
                <p>Expert routing for complex digital, analog, RF, and mixed-signal boards. We optimize for space, signal integrity, and thermals.</p>
            </div>
            <div class="card">
                <h3>DFM & Prototyping</h3>
                <p>Design for Manufacturability (DFM) checks to ensure your board is ready for the fab house. We also handle initial prototyping runs.</p>
            </div>
        </div>
    </section>

    <section class="cta" id="contact">
        <h2 class="section-title">Ready to build your hardware?</h2>
        <p style="color: var(--text-muted); margin-bottom: 2rem;">Send us your project requirements and we'll get back to you with a free consultation and quote.</p>
        <a href="mailto:hello@example.com" class="btn">Contact Us Today</a>
    </section>

    <footer>
        <p>&copy; 2026 CircuitCraft PCB Designs. All rights reserved.</p>
    </footer>

</body>
</html>